package fr.inrae.act.bagap.apiland.core.element;


/**
 * modeling interface of a dynamic feature
 * @author H. BOUSSARD
 * @param <T> the temporal object
 * @param <G> the spatial object
 */
public interface DynamicFeature extends DynamicElement{
	
	
	///**
	// * to get the feature at a given instant
	// * @param t the instant
	// * @return the active feature
	// */
	//Feature getFeature(Instant t);
	
	//void setType(DynamicFeatureType type);
	
}
